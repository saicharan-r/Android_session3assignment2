# Android_session3assignment2

import java.util.scanner;
boolean same;
String s1,s2;
scanner in=new scanner(system.in);
s1=in.nextLine();
s2=in.nextLine();
if(s1==s2)
  same=true;
else
  same=false;
  
